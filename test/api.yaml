openapi: "3.1.0"
  
info:
  title: Tool Call Server API
  description: Tool Call Server API
  version: 0.1.0-alpha

paths:
  /api/v1/test:
    get:
      responses:
        '200':
          description: OK

components:
  schemas:
    test:
      type: object
      properties:
        id:
          type: integer
          format: int64
